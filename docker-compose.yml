version: '3.8'

services:
  frontend-user:
    build:
      context: ./frontend-user
      dockerfile: Dockerfile
    container_name: frontend-user
    ports:
      - "8081:8081"
    restart: unless-stopped
